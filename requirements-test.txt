# Pin Home Assistant to the version this component targets (the version
# running in production). pytest-homeassistant-custom-component pins an exact
# homeassistant version per release, so pip resolves the phacc release that
# matches rather than dragging in a newer beta.
homeassistant==2026.8.3
pytest-homeassistant-custom-component
