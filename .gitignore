__pycache__/
*.pyc
.venv/
.pytest_cache/
.superpowers/

# Local-only: design docs and the verification helper. Deliberately not
# published — they carry environment-specific detail.
docs/superpowers/
script/verify

# Stray screenshots at the repo root (README images live in docs/images)
/*.png

# Local working notes: references specific installation detail
/TODO.md

# Local-only: database backups and the rows extracted from them
/backups/

# The bench writes its results, plans and run file here.
/bench/results/
# A benchmark database and its entries.json, extracted from a backup.
/bench/data/
/bench/cases.yaml
